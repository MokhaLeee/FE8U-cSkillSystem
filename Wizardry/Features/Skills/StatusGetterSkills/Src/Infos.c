#include "gbafe-chax.h"


extern const u16 SI_LifeAndDeath[], SI_DefiantStr[], SI_DefiantMag[], SI_DefiantSkl[], SI_DefiantSpd[], SI_DefiantLck[], SI_DefiantDef[], SI_DefiantRes[], SI_Fury[], SI_FuryPlus[], SI_FortressDef[], SI_FortressRes[];


const struct SkillInfo SkillInfo_LifeAndDeath = {
	
	.msg_desc = ENUM_msg_SD_LifeAndDeath,
	.msg_name = ENUM_msg_SN_LifeAndDeath,
	.icon = SI_LifeAndDeath,
	
};

const struct SkillInfo SkillInfo_Fury = {
	
	.msg_desc = ENUM_msg_SD_Fury,
	.msg_name = ENUM_msg_SN_Fury,
	.icon = SI_Fury,
	
};

const struct SkillInfo SkillInfo_FuryPlus = {
	
	.msg_desc = ENUM_msg_SD_FuryPlus,
	.msg_name = ENUM_msg_SN_FuryPlus,
	.icon = SI_FuryPlus,
	
};

const struct SkillInfo SkillInfo_FortressDef = {
	
	.msg_desc = ENUM_msg_SD_FortressDef,
	.msg_name = ENUM_msg_SN_FortressDef,
	.icon = SI_FortressDef,
	
};

const struct SkillInfo SkillInfo_FortressRes = {
	
	.msg_desc = ENUM_msg_SD_FortressRes,
	.msg_name = ENUM_msg_SN_FortressRes,
	.icon = SI_FortressRes,
	
};


// Defiant Skills

const struct SkillInfo SkillInfo_DefiantStr = {
	
	.msg_desc = ENUM_msg_SD_DefiantStr,
	.msg_name = ENUM_msg_SN_DefiantStr,
	.icon = SI_DefiantStr,
	
};


const struct SkillInfo SkillInfo_DefiantMag = {
	
	.msg_desc = ENUM_msg_SD_DefiantMag,
	.msg_name = ENUM_msg_SN_DefiantMag,
	.icon = SI_DefiantMag,
	
};


const struct SkillInfo SkillInfo_DefiantSkl = {
	
	.msg_desc = ENUM_msg_SD_DefiantSkl,
	.msg_name = ENUM_msg_SN_DefiantSkl,
	.icon = SI_DefiantSkl,
	
};


const struct SkillInfo SkillInfo_DefiantSpd = {
	
	.msg_desc = ENUM_msg_SD_DefiantSpd,
	.msg_name = ENUM_msg_SN_DefiantSpd,
	.icon = SI_DefiantSpd,
	
};


const struct SkillInfo SkillInfo_DefiantLck = {
	
	.msg_desc = ENUM_msg_SD_DefiantMag,
	.msg_name = ENUM_msg_SN_DefiantMag,
	.icon = SI_DefiantMag,
	
};


const struct SkillInfo SkillInfo_DefiantDef = {
	
	.msg_desc = ENUM_msg_SD_DefiantDef,
	.msg_name = ENUM_msg_SN_DefiantDef,
	.icon = SI_DefiantDef,
	
};



const struct SkillInfo SkillInfo_DefiantRes = {
	
	.msg_desc = ENUM_msg_SD_DefiantRes,
	.msg_name = ENUM_msg_SN_DefiantRes,
	.icon = SI_DefiantRes,
	
};