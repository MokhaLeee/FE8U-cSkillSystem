#include "gbafe-chax.h"

// Defiant Skills

extern const u16 SI_DefiantStr[], SI_DefiantMag[], SI_DefiantSkl[], SI_DefiantSpd[], SI_DefiantLck[], SI_DefiantDef[], SI_DefiantRes[];

const struct SkillInfo SkillInfo_DefiantStr = {
	
	.msg_desc = ENUM_msg_SD_DefiantStr,
	.msg_name = ENUM_msg_SN_DefiantStr,
	.icon = SI_DefiantStr,
	
};


const struct SkillInfo SkillInfo_DefiantMag = {
	
	.msg_desc = ENUM_msg_SD_DefiantMag,
	.msg_name = ENUM_msg_SN_DefiantMag,
	.icon = SI_DefiantMag,
	
};


const struct SkillInfo SkillInfo_DefiantSkl = {
	
	.msg_desc = ENUM_msg_SD_DefiantSkl,
	.msg_name = ENUM_msg_SN_DefiantSkl,
	.icon = SI_DefiantSkl,
	
};


const struct SkillInfo SkillInfo_DefiantSpd = {
	
	.msg_desc = ENUM_msg_SD_DefiantSpd,
	.msg_name = ENUM_msg_SN_DefiantSpd,
	.icon = SI_DefiantSpd,
	
};


const struct SkillInfo SkillInfo_DefiantLck = {
	
	.msg_desc = ENUM_msg_SD_DefiantMag,
	.msg_name = ENUM_msg_SN_DefiantMag,
	.icon = SI_DefiantMag,
	
};


const struct SkillInfo SkillInfo_DefiantDef = {
	
	.msg_desc = ENUM_msg_SD_DefiantDef,
	.msg_name = ENUM_msg_SN_DefiantDef,
	.icon = SI_DefiantDef,
	
};



const struct SkillInfo SkillInfo_DefiantRes = {
	
	.msg_desc = ENUM_msg_SD_DefiantRes,
	.msg_name = ENUM_msg_SN_DefiantRes,
	.icon = SI_DefiantRes,
	
};